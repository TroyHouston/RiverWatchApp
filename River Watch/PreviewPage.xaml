<phone:PhoneApplicationPage
    x:Class="River_Watch.PreviewPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait" Orientation="Portrait"
    mc:Ignorable="d"
    shell:SystemTray.IsVisible="True">

    <!--LayoutRoot is the root grid where all page content is placed-->
    <Grid x:Name="LayoutRoot" Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!--TitlePanel contains the name of the application and page title-->
        <StackPanel Grid.Row="0" Margin="12,17,0,28">
            <TextBlock Text="WaiNZ" Style="{StaticResource PhoneTextNormalStyle}"/>
            <TextBlock Text="Preview Page" Margin="9,-7,0,0" Style="{StaticResource PhoneTextTitle1Style}"/>
        </StackPanel>

        <!--ContentPanel - place additional content here-->
        <Grid x:Name="ContentPanel" Grid.Row="1" Margin="12,0,12,0">
            <Image x:Name="previewImage" HorizontalAlignment="Left" Height="186" Margin="82,26,0,0" VerticalAlignment="Top" Width="292" Source="/Assets/Tiles/riverwatchicon.png"/>
            <Button x:Name="uploadButton" Margin="300,439,36,48" VerticalAlignment="Center" Style="{StaticResource roundButton}" Height="120" Click="upload_Click" HorizontalAlignment="Center" Width="120">
                <Button.Background>
                    <ImageBrush  ImageSource="/Assets/Tiles/appbar.close.png" />
                </Button.Background>
            </Button>
            <Button x:Name="cancelButton" Margin="60,439,276,48" VerticalAlignment="Center" Style="{StaticResource roundButton}" Height="120" Click="cancelButton_Click" HorizontalAlignment="Center" Width="120" RenderTransformOrigin="0.56,0.501">
                <!-- <Image Source="/Assets/Tiles/appbar.folder.open.png" HorizontalAlignment="Left" VerticalAlignment="Center" />
                 -->
                <Button.Background>
                    <ImageBrush  ImageSource="/Assets/Tiles/appbar.check.png" />
                </Button.Background>
            </Button>

        </Grid>
        <TextBox x:Name="description" HorizontalAlignment="Left" Height="127" TextWrapping="Wrap" Text="description" VerticalAlignment="Top" Width="456" Margin="14,245,0,0" Grid.Row="1" TextChanged="TextBox_TextChanged"/>
    </Grid>

</phone:PhoneApplicationPage>